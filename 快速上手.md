# 快速上手（简略版教程）

## 安装

1. 双击 FCD-Setup.exe，按提示选择安装目录，完成安装。
2. 桌面会生成「FCD (FUA CODE)」快捷方式，开始菜单同时提供 CLI 与桌面版入口。

免安装用法：直接把 fcd.exe 放到任意目录运行即可（Linux 下载 fcd.elf 后 `chmod +x fcd.elf && ./fcd.elf`）。

## 首次运行

启动后会出现模型选择列表，直接回车选默认的免费模型即可，随后进入对话提示符。

## 基本使用

在 `>` 提示符后直接用中文描述任务：

```
> 解释一下 src/main.py 在做什么
> 把所有的 var 改成 let
> 运行测试并修复失败的用例
```

AI 会自动调用读文件、改文件、执行命令等工具完成任务，回复逐字实时显示。

## 常用命令

对话中可用（以 / 开头，本地解析，不会发给模型）：

```
/models                    查看可选模型
/models <MODEL_ID>         切换默认模型（支持编号/名称）
/add <BASE_URL/v1> <MODEL_ID> [API_KEY]   添加模型（chat/completions）
/delm <MODEL_ID>           删除模型
/usage                     查看时间限额使用情况
/workpath [目录]           显示/指定工作目录
/adm <M|auto|ALL>          审批模式：M 手动审批所有操作 / auto 自动审批 / ALL 完全控制
/tz                        压缩上下文（显示进度）
/mcp                       查看 MCP 服务器
/clear                     清除控制台对话内容
/exit                      退出
```

## 审批模式

- `M`：AI 的每个工具调用（读文件、写文件、执行命令）都要你手动确认。
- `auto`（默认）：只读工具自动执行；写文件、执行命令前需要确认。
- `ALL`：完全控制，所有操作自动执行，适合全自动跑任务。

## 一次性任务

不进交互界面，直接执行一句：

```
fcd --one-shot 检查这个项目有没有语法错误
```

## 额度说明

每个内置免费模型每天 6 小时 AI 生成时长；用量显示在启动界面。额度不影响自带 Key 的付费模型。
