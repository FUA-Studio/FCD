# 使用教程（详细版）

## 1. 安装方式

### Windows 安装版
运行 FCD-Setup.exe，选择安装目录后完成安装。安装内容：

- `fcd.exe`：命令行版
- `fcd-desktop.exe`：原生桌面窗口版
- `uninstall.exe`：卸载器
- 桌面快捷方式与开始菜单项

安装器默认使用当前用户权限，不要求管理员。

### Windows 便携版
fcd.exe 与 fcd-desktop.exe 均为单文件，可放入任意目录直接运行；数据目录自动生成在 exe 旁的 data 文件夹。

### Linux
要求 x86-64 与 python3（建议 Ubuntu 22.04 及以上）：

```
chmod +x fcd.elf
./fcd.elf
```

## 2. 启动参数

```
fcd                      交互式对话
fcd --one-shot <文本>    单次执行，完成后退出
fcd --desktop            启动桌面版
fcd --models             列出已配置模型与 MCP 服务器
fcd --mcp                查看 MCP 服务器
fcd --uninstall          删除配置与数据目录
fcd --help               查看帮助
```

## 3. 对话内内置命令

以下命令由 FCD 本地解析执行，不会发送给模型：

| 命令 | 作用 |
|---|---|
| /help | 显示内置命令 |
| /models | 查看可选模型 |
| /models <MODEL_ID> | 切换默认模型（支持编号/名称） |
| /add <BASE_URL/v1> <MODEL_ID> [API_KEY] | 添加自定义模型（仅支持 chat/completions 端点） |
| /delm <MODEL_ID> | 删除模型（支持编号/名称） |
| /usage | 查看时间限额使用情况与累计生成时长 |
| /workpath [目录] | 显示或指定工作目录 |
| /adm <M\|auto\|ALL> | 切换审批模式（见下） |
| /tz | 手动压缩对话上下文，显示压缩进度与效果 |
| /mcp | 查看 MCP 服务器与启用状态 |
| /clear | 清除控制台对话内容并重置上下文 |
| /exit、/quit | 退出 |

### 审批模式（/adm）

| 模式 | 行为 |
|---|---|
| M | 手动审批所有操作：AI 的每个工具调用都需要确认 |
| auto | 自动审批（默认）：只读工具直接执行；写文件、执行命令前确认 |
| ALL | 完全控制：所有操作自动执行，无需确认 |

上下文过长时，除自动压缩外，可随时输入 /tz 手动压缩并查看进度。

## 4. 模型管理

首次启动自动写入 3 款免费模型：

| 模型 | 特性 | 商用授权 |
|---|---|---|
| Agnes-3.0-Flash | 文本 + 图像 URL 输入 | 可商用 |
| GLM-4.6v-Flash | 视觉模型 | 非商用 |
| GLM-4.7-Flash | 文本 | 非商用 |

免费模型仅供试用与 Demo。投入生产请使用自带 Key 的模型。

### 添加自己的模型

在桌面版「模型」页填写服务商、模型名与 API Key；或直接编辑安装目录下 `data\config.json`。支持：

- DeepSeek（api.deepseek.com）
- 智谱 BigModel（open.bigmodel.cn）
- OpenRouter（一个 Key 调用全部主流模型）
- 任意 OpenAI 兼容 chat/completions 端点（custom 类型，自填 base_url）

Anthropic messages API 端点同样支持流式对话。

### 模型参数

`data\config.json` 中每个模型可设置 `max_context_tokens`（最大上下文）与 `max_output_tokens`（最大输出）；上下文接近上限时 FCD 会自动压缩历史，保证长任务不断档。

## 5. 工具能力

AI 在对话中可自动调用以下工具完成任务：

- 文件：read_file、write_file、edit_file（精准替换）、list_dir、glob_files、grep_search
- 命令：run_cmd（非交互执行，自动捕获输出与退出码）
- 电脑操控（Windows）：screenshot 截屏、mouse_click/double_click/drag、type_text、press_key、scroll、list_windows、activate_window、clipboard_set

电脑操控基于系统原生接口实现，截屏保存于 `data\shots\`。请知悉：默认全自动模式下工具调用无需逐次确认，执行删除等高风险命令前 AI 会先行确认。

## 6. MCP 扩展

MCP（Model Context Protocol）用于接入外部软件的工具能力。配置文件：`data\mcp.json`，将对应服务器条目的 `"enabled"` 改为 true 即启用。内置预设：

blender（3D）、ghidra（逆向）、ida（逆向）、x64dbg（调试）、jadx（APK 反编译）、wireshark（抓包）、yakit（安全测试）、wsl（Linux 子系统命令）、apimonitor（API 监控）、exeinfope（文件识别）。

前提条件：对应软件已在本机安装并运行；uvx / npx 命令可用（需安装 uv 或 Node.js）。启用后 AI 会自动获得这些服务器提供的工具，工具名以 `mcp__` 开头。

## 7. 用量与额度

- 每个免费模型每天 6 小时 AI 生成时长，启动界面显示剩余额度。
- 额度按模型独立计算，用完可切换其他免费模型或使用自带 Key 的模型。
- 启动横幅显示累计 AI 生成总时长。

## 8. 数据目录结构

```
<安装目录>\data\
  config.json    模型与默认模型配置
  usage.json     每日用量与累计时长
  mcp.json       MCP 服务器配置
  sessions\      会话数据
  shots\         截屏图片
```

整个目录便携可迁移；删除即完全重置。

## 9. 卸载

- 安装版：开始菜单「Uninstall FCD」或安装目录内 uninstall.exe，再执行 `fcd --uninstall` 清除数据目录。
- 便携版：删除文件与 data 文件夹即可。
