# 常见问题（FAQ）

## Q1：免费模型可以用多久？
每个内置免费模型每天有 6 小时 AI 生成时长，按模型独立计算，用完可切换其他免费模型。自带 Key 的付费模型不受此限制。

## Q2：提示「无法连接到模型服务」或网络超时？
检查本机网络与代理设置。智谱的免费模型国内可直连；使用海外服务（如 OpenRouter）需要网络可达。企业内网请配置系统代理后重试。

## Q3：对话中报 HTTP 400 或工具调用失败？
多为所选模型不支持 function calling（工具调用）。请更换支持工具调用的模型（内置免费模型均支持），或使用 --clear 清空上下文后重试。

## Q4：杀毒软件提示风险？
FCD 未做代码签名时，部分杀软可能对未知打包程序误报。将安装目录加入白名单即可；生产分发建议对 exe 做代码签名。

## Q5：MCP 服务器一直连不上？
依次检查：对应软件（如 Blender、Ghidra）是否已在本机运行；uvx / npx 是否已安装并在 PATH 中；data\mcp.json 中对应条目是否已改为 "enabled": true。对话内输入 /mcp 可查看各服务器状态与工具数量。

## Q6：AI 会乱动我的电脑吗？
电脑操控（鼠标、键盘、截屏）是真实生效的操作。默认全自动模式下 AI 会直接执行工具；删除文件等高风险命令会先请求确认。请在重要工作前保存文件，或避免让 AI 执行不明确的危险指令。

## Q7：支持哪些模型服务商？
任何提供 OpenAI 兼容 chat/completions 接口的服务均可（DeepSeek、智谱、OpenRouter、本地 Ollama / vLLM 等），Anthropic 接口亦可。在桌面版「模型」页或 data\config.json 中添加。

## Q8：我的配置和数据存在哪里？
全部在安装目录的 data 文件夹内（配置、用量、会话、截屏）。便携可迁移，删除即重置，不写入系统其他位置。

## Q9：如何完全卸载？
先运行安装目录中的 uninstall.exe（或开始菜单的卸载项），再执行 `fcd --uninstall` 删除数据目录；便携版直接删除文件与 data 文件夹。

## Q10：Linux 下运行报错或没反应？
确认系统为 x86-64 且已安装 python3（Ubuntu 22.04 及以上）；确认文件已加执行权限（chmod +x fcd.elf）；若提示缺库，请升级系统到满足 glibc 2.17 以上（近十年的主流发行版均满足）。

## Q11：长对话会不会超出模型上下文？
FCD 会在接近模型上下文上限时自动压缩历史为摘要，保留关键决策与未完成事项；也可以随时输入 /clear 手动重置。

## Q12：上下文里的文件改了，AI 不知道？
直接告诉 AI「重新读取某个文件」，或用 /clear 开启新任务；AI 修改文件前后都会读取最新内容。
