# FCD（FUA CODE）产品简介

FCD，全称 FUA CODE，是由 FUA STUDIO 打造的 AI 编码代理。它以三种形态运行——Windows 命令行、Windows 原生桌面窗口、Linux 命令行——都能直接读懂你的项目、动手修改文件、执行命令、操控电脑，并通过 MCP 协议无限扩展外部工具生态。

---

## 一、双形态原生客户端

| 形态     | 文件              | 特点                                                  |
| -------- | ----------------- | ----------------------------------------------------- |
| 命令行版 | fcd.exe / fcd.elf | 单文件免安装，真终端流式体验，Markdown 按行渲染       |
| 桌面版   | fcd-desktop.exe   | 原生 tkinter 窗口（非浏览器、非 WebView），多页面管理 |

桌面版内含首页、对话、模型、用量、设置、关于六个页面；对话区支持 Markdown 排版与图片附入。两个版本共享同一份配置与数据，交替使用无缝衔接。

### 性能与安全设计

- **真流式**：逐块接收模型输出，终端按行增量渲染，不整段等待。
- **反逆向**：程序体为加密载荷 + HMAC-SHA256 完整性校验 + 反调试检测的引导器，源码不以明文存在。
- **体积克制**：CLI 约 8.6MB、桌面版约 11.2MB、安装器约 19.7MB；无第三方 Python 依赖，连截图都是纯系统 API 实现。
- **便携**：所有数据保存在程序旁的 data 目录，不写注册表（仅卸载信息除外），拷走即迁移。

---

## 二、智能体核心

### 工作循环

FCD 的智能体采用「理解 → 行动 → 验证」循环：每轮可将回复流式输出给你，同时调用多个工具，把执行结果回传给模型继续推理，最多 40 轮直至任务完成。

### 内置工具集

| 类别     | 工具                                            | 说明                                                    |
| -------- | ----------------------------------------------- | ------------------------------------------------------- |
| 文件读取 | read_file / list_dir / glob_files / grep_search | 读取文件、列目录、通配符查找、正则全文搜索              |
| 文件写入 | write_file / edit_file                          | 新建覆盖写入；精准文本替换（可全局替换）                |
| 命令执行 | run_cmd                                         | 非交互执行 shell 命令，捕获输出与退出码，120 秒超时保护 |
| 电脑操控 | 见第四节                                        | 鼠标、键盘、截屏、窗口、剪贴板                          |
| MCP 扩展 | mcp__服务器__工具                               | 动态接入外部 MCP 服务器提供的任意工具                   |

### 上下文管理

- 按模型上下文窗口自动计算字符预算，超预算时裁剪最早的历史。
- 占用超过 80% 预算时**自动压缩**：由模型把中间历史提炼为摘要，保留已完成步骤、关键决策、文件路径与待办。
- 任何时刻可输入 `/tz` **手动压缩**，实时显示压缩前后的消息条数、字符量与耗时。

---

## 三、模型系统

### 开箱即用

首次启动自动写入三款免费模型，免注册免配置：

- **Agnes-3.0-Flash**：新一代文本模型，支持文本与图像 URL 输入，可商用。
- **GLM-4.6v-Flash**：视觉模型，支持图像理解（非商用授权）。
- **GLM-4.7-Flash**：普惠编码模型，国内直连（非商用授权）。

每个免费模型每日 6 小时 AI 生成时长，独立计量；启动界面与 `/usage` 随时可查剩余额度与累计生成时长。

### 自带模型

- 支持任意 **OpenAI 兼容 chat/completions** 端点：DeepSeek、智谱 BigModel、OpenRouter、本地 Ollama / vLLM 等。
- 同时支持 **Anthropic messages API**（流式）。
- 对话内 `/add <BASE_URL/v1> <MODEL_ID> [API_KEY]` 即加即用，`/models` 切换，`/delm` 删除。
- 每个模型可单独配置最大上下文 tokens 与最大输出 tokens。

---

## 四、电脑操控（Windows）

FCD 可以像人一样操作图形界面。全部基于系统原生接口（SendInput / GDI / Win32 API），不依赖任何第三方库：

| 能力   | 工具                                                 | 说明                                                      |
| ------ | ---------------------------------------------------- | --------------------------------------------------------- |
| 截屏   | screenshot                                           | 全屏截取为 PNG，保存至 data\shots，视觉模型可直接读图分析 |
| 鼠标   | mouse_click / double_click / mouse_move / mouse_drag | 左中右键、双击、坐标移动、轨迹拖拽                        |
| 键盘   | type_text / press_key                                | Unicode 逐字输入（支持中文）；组合键如 ctrl+s、alt+tab    |
| 滚动   | scroll                                               | 滚轮上下滚动                                              |
| 窗口   | list_windows / activate_window                       | 枚举全部可见窗口（句柄+标题）、按标题激活置前             |
| 剪贴板 | clipboard_set                                        | 写入任意文本                                              |

典型用途：让 AI 「打开某软件、点击某个按钮、截图确认结果」——它能看屏幕、动鼠标、敲键盘、读窗口，形成闭环。

---

## 五、MCP 扩展生态

FCD 内置完整的 **MCP（Model Context Protocol）客户端**：通过 stdio JSON-RPC 与外部 MCP 服务器通信，自动完成 initialize 握手、工具枚举与调用转发。启用的服务器所提供的全部工具会动态注入对话，模型按需调用，工具名形如 `mcp__<服务器>__<工具>`。

### 内置预设（data\mcp.json 中开启即用）

| 预设       | 用途                 | 前提                          |
| ---------- | -------------------- | ----------------------------- |
| blender    | 3D 建模与场景操作    | Blender 装好 blender-mcp 插件 |
| ghidra     | 二进制逆向分析       | Ghidra 运行中并装 MCP 插件    |
| ida        | 反汇编与逆向         | IDA 加载 MCP 插件             |
| x64dbg     | 动态调试联动         | 调试器侧插件监听              |
| jadx       | Android APK 反编译   | jadx-mcp-server               |
| wireshark  | 抓包分析             | tshark 可用                   |
| yakit      | MITM / 安全测试      | Yakit 联动                    |
| wsl        | Linux 子系统执行命令 | WSL 已安装                    |
| apimonitor | Windows API 监控     | 本机装 API Monitor            |
| exeinfope  | 文件指纹识别         | ExeInfoPE / DIE               |

开启方式：将 `data\mcp.json` 中对应条目的 `"enabled"` 改为 true；对话内输入 `/mcp` 实时查看每个服务器的连接状态与工具数量。没有预设覆盖的服务器，也可在 mcp.json 中自定义 command + args 接入任何 MCP 服务器。

---

## 六、审批与安全

三档审批模式（`/adm` 切换，system prompt 同步告知模型当前权限）：

| 模式 | 行为                                                       |
| ---- | ---------------------------------------------------------- |
| M    | 手动审批所有操作：每个工具调用（含读文件）都需确认         |
| auto | 自动审批（默认）：只读工具直接执行；写文件、执行命令前确认 |
| ALL  | 完全控制：所有操作自动执行，适合全自动跑任务               |

- 高风险命令（删除、格式化、关机等）在 auto 模式下也会额外标记确认。
- 拒绝操作即时反馈给模型，AI 会礼貌停止或换方案。
- 免费模型额度用尽自动拦截，付费模型不受限。

---

## 七、完整命令一览

```
/models                            查看可选模型
/models <MODEL_ID>                 切换默认模型（支持编号/名称）
/add <BASE_URL/v1> <MODEL_ID> [API_KEY]
                                   添加模型（仅支持 chat/completions）
/delm <MODEL_ID>                   删除模型（支持编号/名称）
/usage                             查看时间限额使用情况
/workpath [目录]                   显示/指定工作目录
/adm <M|auto|ALL>                  切换审批模式
/tz                                压缩上下文（显示进度）
/mcp                               查看 MCP 服务器
/clear                             清除控制台对话内容
/exit                              退出 FCD（/quit 等效）
```
---

## 八、技术规格

| 项目       | 规格                                                         |
| ---------- | ------------------------------------------------------------ |
| Windows 版 | fcd.exe（CLI，约 8.6MB）+ fcd-desktop.exe（桌面，约 11.2MB） |
| Linux 版   | fcd.elf（x86-64，glibc 符号 ≤2.17，Ubuntu 22.04+ 实测可用）  |
| 安装器     | NSIS 制作，LZMA 固实压缩，约 19.7MB，免管理员权限            |
| 运行时     | 自带 Python 运行时，零第三方依赖                             |
| 数据位置   | 程序旁 data\（config.json / usage.json / mcp.json / sessions / shots） |
| 保护机制   | 载荷加密 + HMAC 完整性校验 + 反调试                          |

---

*FCD（FUA CODE）· FUA STUDIO*
